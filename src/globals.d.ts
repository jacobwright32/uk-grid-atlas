/** Build-time constants injected by vite.config.ts `define`. */

/** True in normal builds (transmission from PMTiles); false in the
 *  self-contained single-file build (GeoJSON bundles). */
declare const __TILES__: boolean
